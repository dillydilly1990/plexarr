networks:
  VPN:
    name: VPN
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${DOCKER_SUBNET:?err}
          gateway: ${DOCKER_GATEWAY:?err}
include:
  - compose/individual/gluetunVPN.yaml
  - compose/individual/homepage.yaml
  - compose/individual/zurg.yaml